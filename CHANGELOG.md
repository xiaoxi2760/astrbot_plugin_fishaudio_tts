# 更新日志

## [2.1.1] - 2026-08-09

### 新增
- 自动兜底 TTS（`auto_tts_fallback`，默认开启）：AI 在思考里计划调用 `tts_speak` 却没真正发出工具调用时，只要最终回复里写了台词（非旁白/标签/表情/内心独白的正文），也会在发送前自动补合成语音，保证"写了台词就会说话"
- 台词提取工具方法 `_looks_like_rp` / `_extract_spoken_text`：只对带 RP 标记的回复兜底，普通问答不受影响；本轮回合已通过 `tts_speak` 出过声不会重复合成

### 变更
- 强化 `on_llm_request_boost` 注入提示：改为强制规则，明确"写了台词就必须调用 tts_speak"、`text` 只放台词、动作/表情放文字，并提示尽量安排台词开口
- `语音状态` 增加"自动兜底 TTS"开关显示

## [2.1.0] - 2026-08-08

### 新增
- 指令：`语音默认`（管理员切换默认音色，持久化保存，重启后仍生效）、`语音状态`（管理员查看运行状态），均支持斜杠形式 `/语音默认`、`/语音状态`
- 可配置项：字数上限 `max_chars`（默认 500）、最大并发 `max_concurrent_requests`（默认 5）、失败重试 `max_retries`（默认 3）、超时 `timeout_total` / `timeout_connect` / `timeout_sock_read`

### 变更
- 文本超过字数上限时直接回复「字数超限」，不再合成
- `语音状态`、`语音默认` 改为仅管理员可用
- 更新作者与仓库地址信息

### 优化
- 音色匹配使用预排序列表，长名称优先
- 并发信号量控制同时请求数，避免触发官方并发限制
- 日志更详细（字数、耗时、字节数、重试次数等）

## [2.0.0] - 2026-08-08

### 新增
- 基于 FishAudio API 的 TTS 插件，支持多音色克隆
- 自然语言触发：`音色名说 文本`（如 `小爱说 今天天气真不错`）
- LLM 语音工具 `tts_speak`，AI 可主动开口说话，并附带一小段文字回复
- 指令：`语音帮助`、`语音音色`、`/tts`（管理员开关，默认开启）
- 支持 8 种官方情感标签，其他标签原样透传
- 频率限制（管理员豁免）、HTTP 代理、旧版 `reference_id` 向下兼容

### 优化
- 复用 aiohttp 会话、流式写入音频、24 小时缓存自动清理
- 429/5xx 指数退避重试、细分超时
- 修复音频文件大小统计错误
